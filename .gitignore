# Dependencies
node_modules
*/node_modules

# Build outputs
dist
build
*.log
coverage
*.tsbuildinfo

# AWS CDK
cdk.out
*.js
*.d.ts
!jest.config.js

# Environment variables
.env
.env.local
# Vite defaults are committed so fresh clones build valid meta tags (no
# secrets — VITE_* vars are public and overridden by CI)
!.env.development
!.env.production

# IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp
temp

# Frontend specific
frontend/dist
frontend/.env

# Backend specific
backend/dist
backend/.env

# Infrastructure specific
infrastructure/cdk.out
infrastructure/json
infrastructure/.env

# E2E test artifacts
e2e/test-results/

# Project-local Claude rules stay untracked by decision (BK-006, 2026-09-04)
/CLAUDE.md
.claude/

# Playwright root output (legacy, kept on disk but ignored — BK-006)
/test-results/

# Design files (binary)
EstimateNest UI Design.make
tmp-design/
design-extract/

# Backup directories
node_modules.backup/

# Test scripts
backend/test-vote.cjs

# Scripts generated files
scripts/*.log
scripts/*-outputs.json
scripts/domain-setup-outputs.json
infrastructure/cdk.json.backup.*